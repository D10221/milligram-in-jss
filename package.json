{
  "name": "milligram-in-jss",
  "private": true,
  "workspaces": [
    "packages/*",
    "types/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "start": "cd packages/demo && npm start"
  },
  "dependencies": {
    "lerna": "^2.9.0"
  }
}
